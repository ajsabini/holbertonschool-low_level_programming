#include <stdio.h>

/**
 * main - algo
 * Return: siempre 0
 */

int main(void)
{
	/* declaro la variable chr, y le asigno el string a imprimir */
	char Mystr[] = "\"Programming is like building a multilingual puzzle";
	 /*llamo la funcion puts y le paso el parametro*/
	 puts(Mystr);
return (0);
}
