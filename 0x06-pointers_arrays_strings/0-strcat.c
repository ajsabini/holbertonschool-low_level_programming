#include "main.h"
#include <string.h>

/**
 * _strcat - imprime la diagonal
 * @dest: valor
 * @src: segundo valor
 * Return: la copia
 */

char *_strcat(char *dest, char *src)
{
	return (strcat(dest, src));
}
